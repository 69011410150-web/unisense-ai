module {
  // Cross-cutting identifiers and primitives shared across domains.
  public type Id = Nat;
  public type Minutes = Nat;
  public type Metres = Nat;
  public type ClockTime = Text; // "HH:MM" 24-hour demo clock time
  public type DayOfWeek = Text; // "จันทร์" .. "อาทิตย์"
};
